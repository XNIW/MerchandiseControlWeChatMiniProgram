{
  "name": "merchandise-control-wechat-mini-program",
  "version": "0.1.0",
  "private": true,
  "description": "Shop-scoped WeChat Mini Program with read-only sales and controlled catalog management.",
  "engines": {
    "node": "26.7.0",
    "npm": ">=11.19.0 <12"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:test": "node scripts/build-tests.mjs",
    "check:governance": "node scripts/check-governance.mjs",
    "check:secrets": "node scripts/check-secrets.mjs",
    "lint": "biome check .",
    "test": "npm run build:test && node --test dist-test/tests/*.test.js && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "verify": "npm run check:governance && npm run check:privacy && npm run check:secrets && npm run typecheck && npm run lint && npm test && npm run build",
    "check:privacy": "node scripts/sync-privacy.mjs --check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "miniprogram-api-typings": "5.2.3",
    "typescript": "7.0.2"
  }
}
