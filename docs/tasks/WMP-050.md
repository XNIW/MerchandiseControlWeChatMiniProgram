# WMP-050 — Official Mini DevTools runtime and visual QA

- Status: `PLANNING`
- Parent/epic: `WECHAT-007`
- Owner: Mini repository owner and authorized DevTools operator
- Opened / updated: 2026-08-14 / 2026-08-14
- Commit / PR, if any: pending

## Context and objective

Import the current built Mini project into official DevTools with the real
staging AppID, then execute runtime/network/privacy/visual QA without publishing.

## Dependencies and files to read

- WMP-047/048; Mini build/runtime config; official AppID/domains/test membership.

## In scope

- Fresh build/import from repository root and `dist/`; OFF-state and activated
  compile/preview; Auth/error/offline/session states; five tabs, forms, locales,
  images, privacy, Network/Storage/log redaction and simulator/preview device layouts.

## Out of scope

- Upload/review/publication, tracked private DevTools config, fabricated AppID or Excel UI.

## Files potentially involved

- Mini runtime/build configuration, tests and ignored `project.private.config.json`.

## Acceptance criteria

- [ ] Official AppID project imports current `dist/` with no domain-validation bypass.
- [x] Public staging build inputs are validated and substituted into generated `dist/` only; absent input stays OFF.
- [ ] OFF build issues no Auth/gateway/storage request and all tabs fail closed coherently.
- [ ] Activated runtime covers the exact Auth/catalog/sales/history/account/offline/conflict states.
- [ ] Camera/album, privacy/business domains and signed image URL handling are reviewed.
- [ ] Screenshots exclude secrets, PII and signed URL query data.

## Checks and evidence

| Check | Command/method | Result | Duration | Evidence |
|---|---|---|---|---|
| Install/process | notarization, bundle/version and process inspection | `PASS_WITH_UI_LIMITATION` | Recorded | DevTools 2.01.2510290 running; Computer Use state timeout |
| Public build substitution | default/configured build smoke plus full Mini gate | `PASS` | Recorded | validated HTTPS public values; safe defaults; `npm run verify` 63/63 |
| Project compile/preview/visual QA | official DevTools | `NOT_RUN` | — | AppID/config missing |

## Risks and remaining problems

The env template remains outside the repository, but the build now consumes its
approved request/download aliases or explicit public build variables. Runtime
stays OFF and privacy/storage origins stay invalid or empty until real values exist.

## Execution notes and worklog links

No portal automation or domain/TLS bypass is allowed.

## Review decision

`PENDING`

## Next step and handoff

Prepare a reviewed public build substitution only after WMP-047 provides real values.
