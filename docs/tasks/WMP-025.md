# WMP-025 — WeChat DevTools visual QA and final integrated review

- Status: `BLOCKED_EXTERNAL`
- Classification: `DEVTOOLS_AND_AUTHORIZED_RUNTIME_EVIDENCE_REQUIRED`
- Parent/epic: `WECHAT-003`
- Owner: independent reviewer plus authorized WeChat/dev-staging operator; Codex may prepare and execute non-production checks but cannot approve `DONE`
- External blocker owner: authorized WeChat Mini Program operator/reviewer with the registered non-production application and test environment
- Unblock procedure: provide the approved AppID, request/upload/socket domains, DevTools project access, redacted authorized viewer/writer identities and at least one supported device/runtime; import and compile the Mini, execute the required screen/state/locale/network matrix, and attach redacted screenshots, timings and request evidence without enabling production
- Opened / updated: 2026-08-12 / 2026-08-13
- Commit / PR, if any: none

## Context and objective

Review WECHAT-003 as one integrated system after WMP-017…WMP-024: full diffs, server boundaries, automated tests, visual/accessibility QA, performance, security, dependencies, external Auth and live limitations. This task absorbs the intent of the older unstarted WMP-009/WMP-010 backlog for the expanded WECHAT-003 scope; their history is preserved and they are not marked complete.

The complete Mini automated gate passes, but no authorized AppID/domain/device or complete live Auth/convergence evidence is available. The task is therefore `BLOCKED_EXTERNAL`, with review decision `CHANGES_REQUIRED` until the recorded unblock procedure is completed.

## Dependencies and files to read

- WECHAT-001/002/003 and every WMP-017…WMP-024 task/evidence artifact.
- Complete Mini/Admin/Android/iOS diffs, Master Plans, threat/test/parity matrices, ADR-002/ADR-003/DEC-002 and activation runbooks.
- Authorized WeChat DevTools project configuration, registered request/socket domains and non-production test identities.

## In scope

- Full Mini `verify`, typecheck, lint, tests, build, governance, secret and diff/status checks.
- Focused Admin/Supabase/Auth/catalog/image/history/sync/RLS/performance gates and Android/iOS convergence/Auth regression suites.
- DevTools compile and visual QA for Home, Sales, Database, product create/edit/image, categories, suppliers, History, Account, viewer/offline/error/conflict, four locales and small/large screens.
- Approximately 20k-product, broad sales/price/history datasets with keyset/no-N+1/batched-image/cache evidence.
- Dependency audit including the reported `nanoid` warning, with compatible remediation or documented exploitability/plan.
- Final residual risks and exact external activation steps without production, publication or secret exposure.

## Out of scope

- Publication, submission to review, production deployment/migration, payment/contract acceptance, 2FA bypass, commit, push, PR or merge.
- Marking any task `DONE`, treating screenshots/fixtures as live Auth, or hiding global/legacy test failures.

## Files potentially involved

- Governance/evidence documents and redacted screenshots outside the repository or approved evidence paths.
- Application/source changes belong to their preceding task and must return through `FIX`, not be hidden inside final review.

## Acceptance criteria

- [ ] WMP-017…WMP-024 acceptance evidence is complete or every unmet gate is explicitly classified without `DONE`.
- [x] Mini `npm ci` and `npm run verify` pass, including governance, secret scan, typecheck, lint, unit tests and build.
- [x] Admin verify/build and focused Auth/catalog mutation/image/replacement/history/sync tests pass; local pgTAP proves viewer, cross-shop, concurrency, idempotency, archive/restore, price, audit and images.
- [x] Android JVM/lint/assemble and iOS simulator/full suites pass with catalog/image convergence and Auth/Google/session/sync regressions.
- [ ] DevTools compiles and visual/accessibility QA covers all required screens/states/locales/sizes with redacted evidence.
- [ ] Performance evidence proves bounded keyset pages, debounce/cancellation, no full catalog, no N+1, batched signed URLs, lazy thumbnails and bounded caches.
- [x] Secret scans cover source and generated client artifacts; no AppSecret/service-role/session key/token is present.
- [ ] Auth/live invalidation/convergence claims match actual staging/device evidence; unrun gates remain `NOT RUN` with exact owner/unblock step.
- [x] `nanoid` audit status and the pre-existing global pgTAP crash are reported truthfully and not masked by targeted suites.
- [ ] Git diff/status are recorded for every repository; no commit/push/PR/merge, production change or out-of-scope repository modification occurred.

## Checks and evidence

| Check | Command/method | Result | Duration | Evidence |
|---|---|---|---|---|
| Mini clean install/full gate | `npm ci`; `npm run verify` | PASS — install; governance, scoped secret scan, typecheck, Biome, 53/53 tests and build | 0.47 s; 2.68 s | Local implementation log |
| Admin clean install/application gates | `npm ci`; verify/typecheck/lint/i18n/security/build plus focused foundation | PASS — all named gates; focused foundation 47/47 | 8.41 s install; 24.13 s verify | No deployment or production action |
| Supabase local/scoped gates | Local reset; combined scoped, catalog, image and standalone task_149 pgTAP; database lint | PASS — reset 25.75 s; combined 474/474; catalog 81/81; image 35/35; standalone image 162/162; lint 0 error-level findings | 1.76 s standalone; 2.84 s lint | Global multi-file harness separately reproduced the known path crash after 312 PASS |
| Android gates | Convergence/full JVM, lint and assemble | PASS — full 908 total, 5 skipped, 0 failed; lint/assemble PASS | Recorded Android run | No device/live claim |
| iOS gates | Convergence/focused/full and Release simulator build | PASS — 4/4; 76/76; 1296 passed plus 35 expected skips, 0 failed; Release simulator build PASS | Recorded iOS run | Main WeChat provider remains unconfigured; no live claim |
| Performance | Rolled-back local fixtures and `EXPLAIN (ANALYZE, BUFFERS)` | PASS for bounded product/history/price paths; category/supplier counts expose an O(page × products) optimization debt | Recorded local run | No production data; no performance claim beyond captured fixture |
| Dependency audit | Admin and Mini production/full audits | PASS — both production and full trees report 0 | Final WECHAT-004 rerun | `nanoid` 3.3.18; compatible `wrangler` update carries `undici` 7.29.0 |
| DevTools/live QA | Authorized AppID/domains/test identity | NOT RUN — external prerequisites absent | — | Redacted screenshots/logs required |
| Mini diff/security hygiene | Scoped secret scan in `verify`; `git diff --check` | PASS for the local Mini worktree | <1 s diff check | No commit/push/PR/deploy; broad dirty tree still requires attribution review |

## Risks and remaining problems

- DevTools visual success without a real authenticated server session does not prove Auth, RLS, mutation or convergence.
- Broad dirty worktrees require careful attribution; final review must not overwrite or silently include unrelated work.
- External activation, iOS provider, private invalidation and real-device behavior remain release blockers.
- Targeted passing suites do not erase the reproduced global multi-file pgTAP path crash after 312 passing assertions or the three dev-only transitive audit findings.
- An ordinary WeChat Supabase bearer can still reach legacy/table same-shop catalog write sinks outside the controlled mutation lane; write flags remain OFF until coordinated RLS/grant/token scoping closes the bypass.
- Automated Mini success does not prove visual accessibility, real offline recovery, trusted image upload or cross-surface convergence.

## Execution notes and worklog links

The completed native Mini implementation passes its full automated gate with 53/53 tests. Admin application/scoped database and Android/iOS device-free gates also pass with the transparent global-harness and dev-dependency limitations recorded above. WMP-009/WMP-010 remain preserved as historical backlog entries superseded in planning by this expanded task. DevTools/live evidence remains externally blocked and no publication, production action, commit, push or PR occurred.

## Review decision

`CHANGES_REQUIRED`

## Next step and handoff

The authorized WeChat operator must complete the unblock procedure above. In parallel, the independent reviewer can inspect the complete diff and passing Admin/Android/iOS evidence, plus the controlled-lane bypass remediation; no `DONE` transition is permitted.

`DONE` requires explicit user/designated-reviewer confirmation.
