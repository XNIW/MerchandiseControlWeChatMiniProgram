# WMP-015 — Account, shop switch, offline UX and localization

- Parent: `WECHAT-002`
- Status: `REVIEW`

Implementation evidence: own profile/providers/WeChat state, shop selection, local logout, session and membership errors, version, typed four-locale dictionaries, bounded retry/cache UX and sanitized history are present. The privacy/deletion URL is an external runtime value and remains unconfigured.

## Scope

Add account/profile/provider state, active/available shops, local logout, expiry/suspension/removal handling, version/privacy/deletion information, language selection, bounded offline state and recent sanitized sync/history.

## Acceptance criteria

- zh-Hans, English, Italian and Spanish cover every user-facing string.
- Session tokens remain memory-only and local logout clears them.
- Provider and profile state are derived from authorized server responses.
- No technical logs, tokens or cross-shop history are exposed.

## Required tests

Locale completeness, account/shop state, session expiry, offline/retry, sanitized history, revoked membership and client build/governance/secret scans.
