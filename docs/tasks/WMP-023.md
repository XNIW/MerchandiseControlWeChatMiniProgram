# WMP-023 — Remaining WeChat Auth client completion

- Status: `BLOCKED_EXTERNAL`
- Classification: `EXTERNAL_ACTIVATION_REQUIRED`
- Parent/epic: `WECHAT-003`
- Owner: Admin Web owns the Auth boundary and canonical ADR; platform owners own official adapters; external bridge/WeChat/Supabase operators own activation
- External blocker owner: authorized Admin identity/OIDC operator together with the WeChat/Supabase application operators
- Unblock procedure: approve and operate the bridge issuer/discovery/JWKS/rotation contract; configure non-production `custom:wechat`, AppIDs, registered domains/callbacks and redacted test identities; replace the iOS unconfigured provider; then run the four-surface live identity and regression matrix with all feature flags initially OFF
- Opened / updated: 2026-08-12 / 2026-08-13
- Commit / PR, if any: none

## Context and objective

Close only remaining WECHAT-001/002 Auth work that is technically possible without rebuilding the architecture. The Mini already has a feature-gated `wx.login` challenge/exchange consumer and memory-only session contract. Live WeChat login remains unavailable because the approved OIDC bridge, provider/configuration, AppIDs/domains and cross-surface identities are not operational, and the iOS main path still uses `UnconfiguredWeChatAuthorizationCodeProvider`.

Therefore this task cannot be classified as code-complete. Keep all flags OFF, preserve Google/email/session/staff-login regressions, and record exact external and code prerequisites without presenting fixture success as live authentication.

## Dependencies and files to read

- WECHAT-001/002, Admin ADR-002 and identity API contract, external activation runbook and current surface flags.
- Mini `auth-client.ts`, session store, runtime config, account/login states and tests.
- Android official adapter/callback audit and iOS provider/URL/Universal Link audit.
- Official WeChat and Supabase provider/session-handoff documentation.

## In scope

- Mini `wx.login`, one-time challenge/exchange, strict provider handoff, expiry/logout/unlinked/suspended/revoked states and no OpenID bearer use.
- Admin challenge/exchange/callback/linking smoke fixtures, provider OFF behavior and server-only platform-admin/membership checks.
- Android/iOS state, replay, cold/warm callback, timeout/cancel/deny/not-installed and Google/session/sync regression evidence where official adapters are available.
- Exact activation ownership for bridge issuer/discovery/JWKS/rotation, Supabase `custom:wechat`, registered apps/domains/signatures/Universal Link and test identities.

## Out of scope

- Improvised identity provider/JWT, fake email/password, magic link, service-role in clients, nickname-based linking or claiming live authentication without evidence.
- App publication, production provider enablement, secret capture or bypassing 2FA/legal/registration gates.

## Files potentially involved

- Mini Auth/session/account/runtime configuration and tests.
- Admin ADR/contracts/routes/config/fixtures; Android/iOS official adapter and callback-owned files.
- External activation evidence outside the repository.

## Acceptance criteria

- [x] Mini requests a server challenge, obtains only a temporary `wx.login` code, exchanges through the fixed Admin boundary and accepts only `custom:wechat` handoff.
- [x] Mini keeps the bearer memory-only, rejects invalid/expired sessions and clears it on local logout; it never logs/stores OpenID, `session_key`, AppSecret or refresh token.
- [x] Mini feature flag defaults OFF and requires an HTTPS gateway; fixtures are labelled non-live.
- [ ] Admin approved bridge is operational with fixed issuer, discovery, JWKS, rotation, audience/nonce and server-side WeChat code exchange.
- [ ] iOS main path no longer uses `UnconfiguredWeChatAuthorizationCodeProvider` and has an official verified provider/callback configuration.
- [ ] Android/iOS official adapters, package/bundle registration and cold/duplicate/state/cancel/deny/not-installed tests are independently verified.
- [ ] Supabase `custom:wechat`, approved apps/domains and authorized test identities are configured in non-production.
- [ ] A live four-surface login resolves to the same canonical personal account and authorized shops, with Google/email/shop-code and sync regressions green.
- [ ] No AppSecret, `session_key`, token or complete external identifier appears in client bundles, logs or evidence.

## Checks and evidence

| Check | Command/method | Result | Duration | Evidence |
|---|---|---|---|---|
| Mini Auth contract | `npm run verify` including Auth/session fixtures | PASS for the feature-gated Mini consumer; 53/53 tests overall | Final reconciliation run | `miniprogram/lib/auth-client.ts`, `session-store.ts`; `tests/auth-client.test.ts`, `tests/session-and-sales.test.ts` |
| Admin Auth boundary | Verify/focused runtime and database evidence | PASS for the implemented OFF-by-default challenge/exchange boundary and automated fixtures | Recorded cross-repository run | Admin verify/typecheck/lint/i18n/security/build PASS; focused foundation 47/47; local reset PASS |
| Android official adapter/regression | Full JVM suite, lint and assemble | PASS — official OpenSDK adapter present; 908 total, 5 skipped, 0 failed; lint/assemble PASS | Recorded Android run | No live callback/device claim |
| Bridge/provider operation | Approved issuer/discovery/JWKS and Supabase verification | NOT RUN / MISSING | — | Admin ADR-002 classifies bridge unimplemented |
| iOS official provider | Main-path source inspection | FAILING COMPLETION GATE | — | `UnconfiguredWeChatAuthorizationCodeProvider` remains |
| Four-surface live E2E | Authorized staging/devices | NOT RUN — external activation required | — | AppID/domains/bridge/test identity absent |

## Risks and remaining problems

- Missing bridge is a code/runtime security dependency, not only a credential toggle.
- A placeholder client that appears enabled would create a broken or unsafe login path; flags must remain OFF.
- Cross-AppID identity cannot be inferred when UnionID is absent; linking must remain explicit and fail closed.
- Durable refresh in Mini storage lacks an approved threat model and is intentionally not implemented.

## Execution notes and worklog links

The feature-gated Mini Auth consumer and the implemented Admin/Android automated foundations pass. The task is `BLOCKED_EXTERNAL` because the approved bridge/provider/application configuration and authorized identities are not available, while iOS still has a real code gap in its unconfigured provider. No live login was attempted.

## Review decision

`CHANGES_REQUIRED`

## Next step and handoff

The named external operators must complete the unblock procedure above. Only then may an authorized reviewer enable non-production flags and run the four-surface E2E; production remains out of scope.

`DONE` requires explicit user/designated-reviewer confirmation.
