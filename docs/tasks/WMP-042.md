# WMP-042 — Android and iOS native staging activation

- Status: `REVIEW`
- Parent/epic: `WECHAT-006`
- Owner: Android/iOS repository owners
- Opened / updated: 2026-08-13 / 2026-08-14
- Commit / PR, if any: iOS commits `2a632d27` + `d91cb29a`, PR #7, normal merge `6571f4b661c4b815cfad4f6ad9139071b978f76d`; closeout PR #8, merge `045d0597e13f6d663547a166f74b4e5f498572ea`; Android no source change

## Context and objective

Complete official, environment-driven native adapters and staging configuration,
preserving existing non-WeChat Auth and sync behavior. Device-free work proceeds
even when the physical WeChat devices or approved AppIDs are unavailable.

## Dependencies and files to read

- WMP-041 applications/provider; current official Tencent SDK provenance;
  Android/iOS repository governance and existing WECHAT Auth foundations.

## In scope

- iOS official SDK/provider/registration/URL scheme/Universal Link/callback and
  negative flows; Android current SDK/app/package/fingerprint/callback audit and
  only justified updates; staging gateway/flags; unit/build/simulator/bundle/APK
  secret scans; physical live Auth/sync when devices exist.

## Out of scope

- AppSecret in bundles; store submission; unrelated native refactors; automatic
  Android SDK upgrade without a relevant compatibility/security reason.

## Files potentially involved

- iOS application/Auth/config/project/tests/docs; Android only if evidence proves a necessary bounded fix.

## Acceptance criteria

- [x] Official current SDK versions and provenance are verified.
- [x] iOS no longer has an avoidable placeholder provider; config remains public/environment-driven and flag-gated.
- [x] Cold/warm/duplicate/cancel/deny/state/replay/expiry/uninstalled/backend/session/logout flows have automated coverage.
- [x] Android package/fingerprint/callback/current SDK configuration is exact and green.
- [x] Native full gates and APK/bundle secret scans pass.
- [x] Physical live tests pass or name the exact device/AppID prerequisite without blocking device-free completion.

## Checks and evidence

| Check | Command/method | Result | Duration | Evidence |
|---|---|---|---|---|
| iOS provider/build/tests | official Tencent `2.0.7` + Xcode gates | PASS_DEVICE_FREE | Recorded | focused 12/12; full 1,336 + 4 UI, 36 opt-in skips, 0 failure; Release/Analyze/secret/launch pass; PR CI `31761529498` PASS; PR #7 merged normally |
| Android audit/build/tests | official docs + Gradle repository gates | PASS_NO_CHANGE | Recorded | SDK 6.8.34 retained; 18 focused tests, assembleDebug and lintDebug pass; source clean |

## Risks and remaining problems

Universal Link/AppID approval and installed WeChat apps require real accounts and physical devices.

## Execution notes and worklog links

Use isolated worktrees; preserve the dirty Android TASK-137 checkout unchanged.
The iOS official artifact is labelled `NoPay`, pinned with verified hashes and
compiled with `BUILD_WITHOUT_PAY=1`; the app integrates login only and remains
fail-closed without exact AppID scheme/Universal Link. Android 6.8.34 was not
upgraded because no demonstrated compatibility/security need exists.

## Review decision

`DEVICE_FREE_PASS_EXTERNAL_ACTIVATION_PENDING` — moved to REVIEW after the iOS
provider and closeout PRs merged normally. WMP-041 must still supply real public
identifiers before physical Auth can run.

## Next step and handoff

Integrate device-free fixes normally, then run physical Auth when prerequisites exist.
