# WMP-016 — Cross-platform integration review and release readiness

- Parent: `WECHAT-002`
- Status: `REVIEW / CHANGES_REQUIRED`

Review evidence: the capability matrix and repository gates were executed. Targeted/new gates pass; external live/device/DevTools gates are unavailable, WMP-012 is incomplete, and the unrelated global pgTAP recovery crash remains. No release-readiness claim is made.

## Scope

Reconcile Android/iOS navigation with the Mini Program capability matrix; verify all repository gates, Auth adapters, runtime configuration, cross-shop boundaries, realistic data performance, visual QA and external activation prerequisites.

## Acceptance criteria

- `docs/PARITY_MATRIX.md` covers every mandated core module and all exclusions.
- No task is marked `DONE`; unrun device/live/staging checks remain explicit.
- No AppSecret/service-role value is present in client bundles or logs.
- Final classification follows the WECHAT-002 evidence gates exactly.

## Required tests

All Admin/Supabase/Android/iOS/Mini Program gates requested by WECHAT-002, plus `git diff --check`, secret scans and status/branch evidence. Device, DevTools and live E2E require registered external configuration.
